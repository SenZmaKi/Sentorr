export enum Page {
  Home = "Home",
  Search = "Search",
  Preview = "Preview",
  Player = "Player",
  Settings = "Settings",
  About = "About",
}

export enum MediaCardType {
  Complex = "Complex",
  Simple = "Simple",
}
